name: CI

on:
  push:
    branches:
      - main
  pull_request:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  verify:
    runs-on: ubuntu-24.04
    steps:
      - name: Configure Git defaults
        run: git config --global init.defaultBranch main

      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.23.0'
          cache: false

      - name: Test
        run: go test ./... -count=1

      - name: Vet
        run: go vet ./...

      - name: Module tidy check
        run: go mod tidy -diff

      - name: Race tests
        run: go test -race ./... -count=1

      - name: Build CLI
        run: go build ./cmd/opentunnel

  build-artifacts:
    runs-on: ubuntu-24.04
    strategy:
      matrix:
        include:
          - goos: linux
            goarch: amd64
            name: opentunnel-linux-amd64
          - goos: linux
            goarch: arm64
            name: opentunnel-linux-arm64
          - goos: linux
            goarch: arm
            goarm: '7'
            name: opentunnel-linux-armv7
          - goos: darwin
            goarch: arm64
            name: opentunnel-darwin-arm64
    steps:
      - name: Configure Git defaults
        run: git config --global init.defaultBranch main

      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.23.0'
          cache: false

      - name: Build artifact
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          CGO_ENABLED: '0'
        run: |
          mkdir -p dist
          if [ -n "${{ matrix.goarm }}" ]; then
            GOARM=${{ matrix.goarm }} go build -o dist/${{ matrix.name }} ./cmd/opentunnel
          else
            go build -o dist/${{ matrix.name }} ./cmd/opentunnel
          fi

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.name }}
          path: dist/${{ matrix.name }}
